name: Auto-update OCI CLI package

# Checks daily for a new Oracle Cloud Infrastructure CLI release (via the
# oracle/oci-cli GitHub releases API), and if found: updates the package files,
# packs, pushes to the Chocolatey community feed, and commits the bump back.
#
# Requires one repository secret: CHOCO_API_KEY (your push API key from
# https://community.chocolatey.org/account).

on:
  schedule:
    - cron: '0 7 * * *'    # daily at 07:00 UTC
  workflow_dispatch: {}     # allow manual runs from the Actions tab

jobs:
  update:
    runs-on: windows-latest
    permissions:
      contents: write       # to commit the version bump back
    steps:
      - uses: actions/checkout@v7

      - name: Update and pack if a new version exists
        id: update
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./update.ps1
          $changed = -not [string]::IsNullOrWhiteSpace((git status --porcelain -- oci-cli.nuspec tools/chocolateyinstall.ps1 tools/VERIFICATION.txt))
          $v = ([xml](Get-Content oci-cli.nuspec)).package.metadata.version
          "changed=$($changed.ToString().ToLowerInvariant())" >> $env:GITHUB_OUTPUT
          "version=$v" >> $env:GITHUB_OUTPUT

      - name: Commit the version bump back to the repo
        if: steps.update.outputs.changed == 'true'
        shell: pwsh
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add oci-cli.nuspec tools/chocolateyinstall.ps1 tools/VERIFICATION.txt
          git commit -m "Update OCI CLI to $env:OCI_VERSION"
          # A re-run checks out the original head SHA, so main can already carry
          # this bump from the first attempt. Rebase before pushing.
          git pull --rebase
          git push
        env:
          OCI_VERSION: ${{ steps.update.outputs.version }}

      - name: Push package to Chocolatey
        if: steps.update.outputs.changed == 'true'
        shell: pwsh
        env:
          CHOCO_API_KEY: ${{ secrets.CHOCO_API_KEY }}
          OCI_VERSION: ${{ steps.update.outputs.version }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:CHOCO_API_KEY)) {
              throw "CHOCO_API_KEY repository secret is not set."
          }
          $nupkg = "oci-cli.$env:OCI_VERSION.nupkg"
          if (-not (Test-Path $nupkg)) { throw "Missing package artifact: $nupkg" }
          Write-Host "Pushing $nupkg ..."
          # push.chocolatey.org returns a transient 503 often enough to matter, and
          # by this point the version bump is already committed, so a lost push
          # leaves the package unpublished until the next upstream release.
          foreach ($attempt in 1..3) {
              choco push $nupkg --source https://push.chocolatey.org/ --api-key $env:CHOCO_API_KEY
              if ($LASTEXITCODE -eq 0) { break }
              if ($attempt -lt 3) {
                  Write-Host "Push attempt $attempt failed. Retrying in 60s ..."
                  Start-Sleep -Seconds 60
              }
          }
          if ($LASTEXITCODE -ne 0) {
              throw "choco push failed 3 times. If this was transient, re-publish with the 'Publish current version' workflow. Otherwise check that CHOCO_API_KEY belongs to a Chocolatey account allowed to push package id 'oci-cli'."
          }
